<%= link_to page.name, page %>
<% if page.has_children? %>
    <ul>
      <% for child_page in page.children %>
          <li <%= "class=active" if @page == child_page %> >
            <%= render "menu_item", page: child_page %>
          </li>
      <% end %>
    </ul>
<% end %>
