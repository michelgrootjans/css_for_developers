<% content_for :title, @page.name %>


<%= render 'root_menu', current_page: @page %>

<%= render 'submenu', current_page: @page %>

<%= render 'breadcrumbs', current_page: @page %>

<%= render 'notice' %>

<%= render 'content', current_page: @page %>